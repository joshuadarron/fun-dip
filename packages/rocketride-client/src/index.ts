// Wrapped RocketRide SDK client. Populated in Phase 1 per
// `.claude/docs/PIPELINE_CONTRACTS.md`. Includes one function per pipeline
// and HMAC signer/verifier helpers for callback payloads.
export {};
